---
"next-sidebars": minor
---

Add a `persist` prop to opt sidebars out of localStorage persistence. An ephemeral sidebar always loads as `defaultOpen` — right for mobile overlay sidebars, which shouldn't reopen on reload.
